<Image source={require('../assets/undraw_medicine_b1ol.png')} style={styles.image} />
      <View style={styles.card}>
        <Text style={styles.label}>Name</Text>
        <TextInput
          style={styles.input}
          value={patientData.name}
          onChangeText={(text) => setPatientData({ ...patientData, name: text })}
          placeholder="Enter name"
        />
        <Text style={styles.label}>Age</Text>
        <TextInput
          style={styles.input}
          value={patientData.age}
          onChangeText={(text) => setPatientData({ ...patientData, age: text })}
          placeholder="Enter age"
          keyboardType="numeric"
        />
        <Text style={styles.label}>Gender</Text>
        <TextInput
          style={styles.input}
          value={patientData.gender}
          onChangeText={(text) => setPatientData({ ...patientData, gender: text })}
          placeholder="Enter gender (M/F)"
        />
        <Text style={styles.label}>Location</Text>
        <TextInput
          style={styles.input}
          value={patientData.location}
          onChangeText={(text) => setPatientData({ ...patientData, location: text })}
          placeholder="Enter location"
        />
       
        <Text style={styles.label}>Status</Text>
        <TextInput
          style={styles.input}
          value={patientData.status}
          onChangeText={(text) => setPatientData({ ...patientData, status: text })}
          placeholder="Enter status (New/Returning)"
        />
        <TouchableOpacity style={styles.submitButton} onPress={handleSubmit}>
          <Text style={styles.submitButtonText}>Submit</Text>
        </TouchableOpacity>
      </View>
    </ScrollView>

    // État pour les données de la prescription
  const [prescriptionData, setPrescriptionData] = useState({
    medication: '',
    dosage: '',
    start_date: '',
    end_date: '',
    contraindications: '',
  });